# 🚀 Настройка бота на VPS сервере

## 📋 Требования к серверу:
- **OS**: Ubuntu 20.04+ или CentOS 7+
- **RAM**: 1GB+ (рекомендуется 2GB)
- **CPU**: 1 ядро+
- **Диск**: 10GB+ SSD
- **Python**: 3.8+

## 🔧 Пошаговая настройка:

### 1. **Подключение к серверу:**
```bash
ssh root@your-server-ip
```

### 2. **Обновление системы:**
```bash
apt update && apt upgrade -y
```

### 3. **Установка Python и зависимостей:**
```bash
apt install python3 python3-pip python3-venv git -y
```

### 4. **Создание директории для бота:**
```bash
mkdir -p /root/smartbot
cd /root/smartbot
```

### 5. **Загрузка кода бота:**
```bash
# Вариант 1: Клонирование из Git
git clone https://github.com/your-repo/smartbot.git .

# Вариант 2: Загрузка файлов через SCP
# scp -r /path/to/smartbot/* root@your-server-ip:/root/smartbot/
```

### 6. **Настройка конфигурации:**
```bash
nano config.env
# Добавьте ваши токены:
# VK_TOKEN=your_vk_token
# VK_GROUP_ID=233388296
# DEEPSEEK_API_KEY=your_deepseek_key
```

### 7. **Установка зависимостей:**
```bash
pip3 install -r requirements.txt
```

### 8. **Настройка автозапуска (systemd):**
```bash
# Копируем service файл
cp smartbot.service /etc/systemd/system/

# Перезагружаем systemd
systemctl daemon-reload

# Включаем автозапуск
systemctl enable smartbot

# Запускаем бота
systemctl start smartbot
```

### 9. **Проверка работы:**
```bash
# Статус бота
systemctl status smartbot

# Логи бота
journalctl -u smartbot -f

# Перезапуск бота
systemctl restart smartbot

# Остановка бота
systemctl stop smartbot
```

## 🔍 Мониторинг:

### **Проверка статуса:**
```bash
systemctl status smartbot
```

### **Просмотр логов:**
```bash
# Все логи
journalctl -u smartbot

# Последние логи
journalctl -u smartbot -f

# Логи за последний час
journalctl -u smartbot --since "1 hour ago"
```

### **Перезапуск:**
```bash
systemctl restart smartbot
```

## 🛠 Управление:

### **Команды управления:**
```bash
# Запуск
systemctl start smartbot

# Остановка
systemctl stop smartbot

# Перезапуск
systemctl restart smartbot

# Статус
systemctl status smartbot

# Включить автозапуск
systemctl enable smartbot

# Отключить автозапуск
systemctl disable smartbot
```

## 📊 Мониторинг ресурсов:

### **Использование памяти:**
```bash
ps aux | grep python
```

### **Использование диска:**
```bash
df -h
```

### **Логи системы:**
```bash
tail -f /var/log/syslog
```

## 🔒 Безопасность:

### **Настройка файрвола:**
```bash
ufw allow ssh
ufw allow 80
ufw allow 443
ufw enable
```

### **Резервное копирование:**
```bash
# Создание бэкапа
tar -czf smartbot-backup-$(date +%Y%m%d).tar.gz /root/smartbot/

# Восстановление
tar -xzf smartbot-backup-YYYYMMDD.tar.gz -C /
```

## 🚨 Устранение неполадок:

### **Бот не запускается:**
1. Проверьте токены в `config.env`
2. Проверьте логи: `journalctl -u smartbot`
3. Проверьте права доступа к файлам

### **Ошибки Python:**
1. Установите зависимости: `pip3 install -r requirements.txt`
2. Проверьте версию Python: `python3 --version`

### **Проблемы с сетью:**
1. Проверьте подключение к интернету
2. Проверьте настройки файрвола
3. Проверьте DNS: `nslookup api.deepseek.com`

## 📈 Рекомендации:

1. **Регулярно обновляйте** систему и зависимости
2. **Мониторьте логи** на предмет ошибок
3. **Создавайте бэкапы** конфигурации
4. **Используйте мониторинг** (например, htop, iotop)
5. **Настройте уведомления** о сбоях

## 💰 Рекомендуемые VPS провайдеры:

- **Timeweb** - 200₽/месяц (Россия)
- **Beget** - 300₽/месяц (Россия)
- **Reg.ru** - 250₽/месяц (Россия)
- **DigitalOcean** - $5/месяц (США)
- **Vultr** - $3.5/месяц (США)
